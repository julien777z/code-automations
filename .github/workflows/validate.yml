name: Validate

on:
  push:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-env
        with:
          post_setup_command: poetry install
      - run: poetry run pytest
      - run: poetry run ruff check .
      - run: poetry run ruff format --check .
      - run: poetry run pylint src tests linting/python-rules-lint/src linting/python-rules-lint/tests
      - run: poetry run cloud-automations validate
      - run: go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
      - run: |
          actionlint_path="$(go env GOPATH)/bin/actionlint"
          "$actionlint_path"
