{
  "$defs": {
    "AutomationConfig": {
      "additionalProperties": false,
      "description": "Define one automation.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "prompt": {
          "title": "Prompt",
          "type": "string"
        },
        "schedule": {
          "anyOf": [
            {
              "$ref": "#/$defs/ScheduleConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "skills": {
          "items": {
            "type": "string"
          },
          "title": "Skills",
          "type": "array"
        }
      },
      "required": [
        "prompt"
      ],
      "title": "AutomationConfig",
      "type": "object"
    },
    "ProjectConfig": {
      "additionalProperties": false,
      "description": "Group repositories and automations for one project.",
      "properties": {
        "automations": {
          "additionalProperties": {
            "$ref": "#/$defs/AutomationConfig"
          },
          "minProperties": 1,
          "title": "Automations",
          "type": "object"
        },
        "repositories": {
          "additionalProperties": {
            "$ref": "#/$defs/RepositoryConfig"
          },
          "minProperties": 1,
          "title": "Repositories",
          "type": "object"
        }
      },
      "required": [
        "repositories",
        "automations"
      ],
      "title": "ProjectConfig",
      "type": "object"
    },
    "RepositoryConfig": {
      "additionalProperties": false,
      "description": "Configure one repository within a project.",
      "properties": {
        "branch": {
          "default": "main",
          "title": "Branch",
          "type": "string"
        }
      },
      "title": "RepositoryConfig",
      "type": "object"
    },
    "ScheduleConfig": {
      "additionalProperties": false,
      "description": "Define a timezone-aware POSIX cron schedule.",
      "properties": {
        "cron": {
          "title": "Cron",
          "type": "string"
        },
        "timezone": {
          "title": "Timezone",
          "type": "string"
        }
      },
      "required": [
        "cron",
        "timezone"
      ],
      "title": "ScheduleConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Define the complete automation configuration.",
  "properties": {
    "projects": {
      "additionalProperties": {
        "$ref": "#/$defs/ProjectConfig"
      },
      "minProperties": 1,
      "title": "Projects",
      "type": "object"
    },
    "version": {
      "const": 1,
      "title": "Version",
      "type": "integer"
    }
  },
  "required": [
    "version",
    "projects"
  ],
  "title": "AutomationsConfig",
  "type": "object"
}
